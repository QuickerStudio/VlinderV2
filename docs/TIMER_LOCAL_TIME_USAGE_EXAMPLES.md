# Timer Local Time 使用示例

## 基本用法

### 示例 1：显示当地时间（最简单）

```xml
<tool name="timer">
  <showLocalTime>true</showLocalTime>
</tool>
```

**返回结果：**
```
Current local time: 2025-10-06 14:30:45
```

**UI 显示：**
- 标题栏：`Local Time • 2025-10-06 14:30:45`
- 展开后：居中显示大号时间 `2025-10-06 14:30:45`

---

### 示例 2：带备注的当地时间

```xml
<tool name="timer">
  <showLocalTime>true</showLocalTime>
  <note>Task started at this time</note>
</tool>
```

**返回结果：**
```
Current local time: 2025-10-06 14:30:45 (Task started at this time)
```

**UI 显示：**
- 标题栏：`Local Time • 2025-10-06 14:30:45 • Task started at this time`
- 展开后：
  - 居中显示大号时间 `2025-10-06 14:30:45`
  - 备注：`Note: Task started at this time`

---

### 示例 3：记录任务开始时间

```xml
<tool name="timer">
  <showLocalTime>true</showLocalTime>
  <note>Build process started</note>
</tool>
```

**使用场景：**
在开始一个长时间任务前，记录开始时间点。

---

### 示例 4：对比 - 传统倒计时模式

```xml
<tool name="timer">
  <duration>60</duration>
  <note>Wait 1 minute</note>
</tool>
```

**返回结果：**
```
Timer completed. Waited for 60 seconds. Note: Wait 1 minute
```

**UI 显示：**
- 标题栏：`Timer • 00:00:59:500 • 50% • Wait 1 minute`
- 展开后：显示倒计时、进度条、开始时间、结束时间等

---

## 实际应用场景

### 场景 1：记录部署开始时间

```xml
<!-- 记录部署开始时间 -->
<tool name="timer">
  <showLocalTime>true</showLocalTime>
  <note>Deployment started</note>
</tool>

<!-- 执行部署操作 -->
<tool name="execute_command">
  <command>npm run deploy</command>
</tool>

<!-- 记录部署结束时间 -->
<tool name="timer">
  <showLocalTime>true</showLocalTime>
  <note>Deployment completed</note>
</tool>
```

---

### 场景 2：记录测试执行时间

```xml
<!-- 记录测试开始时间 -->
<tool name="timer">
  <showLocalTime>true</showLocalTime>
  <note>Test suite started</note>
</tool>

<!-- 运行测试 -->
<tool name="execute_command">
  <command>npm test</command>
</tool>

<!-- 记录测试结束时间 -->
<tool name="timer">
  <showLocalTime>true</showLocalTime>
  <note>Test suite completed</note>
</tool>
```

---

### 场景 3：标记重要操作时间点

```xml
<!-- 记录数据库备份时间 -->
<tool name="timer">
  <showLocalTime>true</showLocalTime>
  <note>Database backup initiated</note>
</tool>

<!-- 执行备份 -->
<tool name="execute_command">
  <command>pg_dump mydb > backup.sql</command>
</tool>
```

---

### 场景 4：混合使用倒计时和时间记录

```xml
<!-- 记录任务开始时间 -->
<tool name="timer">
  <showLocalTime>true</showLocalTime>
  <note>Server restart initiated</note>
</tool>

<!-- 等待服务器重启（倒计时） -->
<tool name="timer">
  <duration>30</duration>
  <note>Waiting for server to restart</note>
</tool>

<!-- 记录任务完成时间 -->
<tool name="timer">
  <showLocalTime>true</showLocalTime>
  <note>Server restart completed</note>
</tool>
```

---

## UI 对比

### 当地时间模式 UI

```
┌─────────────────────────────────────────────────────┐
│ 🕐 Local Time                                       │
│ 2025-10-06 14:30:45 • Task started                 │
│                                                     │
│ [展开后]                                            │
│                                                     │
│              Local Time                             │
│         2025-10-06 14:30:45                        │
│                                                     │
│         Note: Task started                          │
└─────────────────────────────────────────────────────┘
```

**特点：**
- ✅ 静态显示，不实时更新
- ✅ 无停止按钮
- ✅ 无进度条
- ✅ 简洁清晰

---

### 倒计时模式 UI

```
┌─────────────────────────────────────────────────────┐
│ 🕐 Timer                                      [X]   │
│ 00:00:45:230 • 75% • Wait 1 minute                 │
│                                                     │
│ [展开后]                                            │
│                                                     │
│ Time Remaining:              00:00:45:230          │
│ ████████████████░░░░░░░░ 75%                       │
│                                                     │
│ Total Duration: 60s (0h 1m 0s)                     │
│ Start Time: 2025-10-06 14:30:00                    │
│ Expected End Time: 2025-10-06 14:31:00             │
│ Note: Wait 1 minute                                 │
└─────────────────────────────────────────────────────┘
```

**特点：**
- ✅ 实时倒计时
- ✅ 有停止按钮
- ✅ 有进度条
- ✅ 显示详细信息

---

## 参数组合

| showLocalTime | duration | 行为 |
|---------------|----------|------|
| `true` | 任意值或省略 | 显示当地时间（静态） |
| `false` 或省略 | > 0 | 倒计时模式 |
| `false` 或省略 | 0 或省略 | 错误：需要提供 duration |

---

## 常见问题

### Q1: 当地时间会实时更新吗？
**A:** 不会。显示的是调用工具时获取到的时间，是静态的。

### Q2: 可以同时使用 showLocalTime 和 duration 吗？
**A:** 可以，但当 `showLocalTime=true` 时，`duration` 参数会被忽略。

### Q3: 如何获取实时更新的时间？
**A:** 当地时间模式是静态显示。如果需要实时时间，可以多次调用该工具。

### Q4: 时间格式可以自定义吗？
**A:** 目前固定为 `YYYY-MM-DD HH:MM:SS` 格式。

### Q5: 显示的是哪个时区的时间？
**A:** 显示的是运行环境的本地时区时间。

---

## 最佳实践

1. **记录时间点**：使用 `showLocalTime=true` 记录重要操作的时间点
2. **等待操作**：使用 `duration` 参数进行倒计时等待
3. **添加备注**：始终使用 `note` 参数说明时间记录的目的
4. **混合使用**：根据需求灵活组合使用两种模式

---

## 总结

Timer 工具现在支持两种模式：

1. **当地时间模式** (`showLocalTime=true`)
   - 静态显示获取时的时间
   - 用于记录时间点
   - 简洁的 UI

2. **倒计时模式** (传统模式)
   - 实时倒计时
   - 用于等待操作
   - 完整的进度信息

两种模式可以根据实际需求灵活使用！

