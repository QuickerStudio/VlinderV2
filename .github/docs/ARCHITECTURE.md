# 自动化发布系统架构

本文档描述 Vlinder 扩展的自动化发布系统架构。

## 📐 系统架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                          开发者工作流                                  │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    │             │             │
                    ▼             ▼             ▼
            ┌──────────┐  ┌──────────┐  ┌──────────┐
            │ 推送代码  │  │ 创建 PR  │  │ 推送标签  │
            └─────┬────┘  └─────┬────┘  └─────┬────┘
                  │             │             │
                  └─────────────┼─────────────┘
                                │
┌─────────────────────────────────────────────────────────────────────┐
│                        GitHub Actions                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌────────────────────────┐      ┌────────────────────────┐        │
│  │   CI 工作流 (ci.yml)    │      │ Release 工作流         │        │
│  │                        │      │ (release.yml)          │        │
│  ├────────────────────────┤      ├────────────────────────┤        │
│  │ 1. Setup Environment   │      │ 1. Setup Environment   │        │
│  │    - Node.js 20        │      │    - Node.js 20        │        │
│  │    - pnpm 8            │      │    - pnpm 8            │        │
│  │    - Cache             │      │    - Cache             │        │
│  │                        │      │                        │        │
│  │ 2. Install             │      │ 2. Install             │        │
│  │    - pnpm install:all  │      │    - pnpm install:all  │        │
│  │                        │      │                        │        │
│  │ 3. Build               │      │ 3. Build               │        │
│  │    - build:webview     │      │    - build:webview     │        │
│  │    - check-types       │      │    - check-types       │        │
│  │    - lint              │      │    - lint              │        │
│  │    - package           │      │    - package           │        │
│  │    - build VSIX        │      │    - build VSIX        │        │
│  │                        │      │                        │        │
│  │ 4. Test                │      │ 4. Create Release      │        │
│  │    - pnpm test         │      │    - GitHub Release    │        │
│  │                        │      │    - Upload VSIX       │        │
│  │ 5. Upload Artifact     │      │                        │        │
│  │    - 30 days           │      │ 5. Publish (Optional)  │        │
│  │                        │      │    - VS Code Market    │        │
│  └────────────────────────┘      │    - Open VSX          │        │
│                                  │                        │        │
│                                  │ 6. Upload Artifact     │        │
│                                  │    - 90 days           │        │
│                                  └────────────────────────┘        │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    │             │             │
                    ▼             ▼             ▼
            ┌──────────┐  ┌──────────┐  ┌──────────┐
            │ GitHub   │  │ VS Code  │  │ Open VSX │
            │ Release  │  │ Market   │  │ Registry │
            └──────────┘  └──────────┘  └──────────┘
```

---

## 🔄 工作流触发机制

### CI 工作流触发

```
Push to Branch
    │
    ├─ main/master/develop
    │       │
    │       └─→ Trigger CI Workflow
    │
Pull Request
    │
    └─ to main/master/develop
            │
            └─→ Trigger CI Workflow
```

### Release 工作流触发

```
Push Tag
    │
    ├─ v*.*.* (正式版)
    │       │
    │       └─→ Trigger Release Workflow
    │               │
    │               └─→ Auto Publish to Marketplace
    │
    ├─ v*.*.*-beta.* (Beta 版)
    │       │
    │       └─→ Trigger Release Workflow
    │               │
    │               └─→ GitHub Release Only
    │
    └─ v*.*.*-alpha.* (Alpha 版)
            │
            └─→ Trigger Release Workflow
                    │
                    └─→ GitHub Release Only

Manual Trigger
    │
    └─→ Trigger Release Workflow
            │
            ├─ publish_marketplace = true
            │       │
            │       └─→ Publish to Marketplace
            │
            └─ publish_marketplace = false
                    │
                    └─→ GitHub Release Only
```

---

## 🏗️ 构建流程详解

### 阶段 1: 环境准备

```
┌─────────────────────────────────────┐
│ 1. Checkout Code                    │
│    - fetch-depth: 0                 │
│    - 获取完整 Git 历史               │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│ 2. Setup Node.js                    │
│    - Version: 20                    │
│    - 配置 npm registry              │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│ 3. Setup pnpm                       │
│    - Version: 8                     │
│    - 配置 store path                │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│ 4. Setup Cache                      │
│    - Key: pnpm-lock.yaml hash       │
│    - 加速依赖安装                    │
└─────────────────────────────────────┘
```

### 阶段 2: 依赖安装

```
┌─────────────────────────────────────┐
│ pnpm install:all                    │
│                                     │
│ ├─ extension/                       │
│ │  └─ pnpm install                  │
│ │                                   │
│ └─ extension/webview-ui-vite/       │
│    └─ pnpm install                  │
└─────────────────────────────────────┘
```

### 阶段 3: 构建

```
┌─────────────────────────────────────┐
│ 1. Build Webview                    │
│    - cd webview-ui-vite             │
│    - pnpm run build                 │
│    - Vite 构建 React UI             │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│ 2. Check Types                      │
│    - tsc --noEmit                   │
│    - TypeScript 类型检查             │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│ 3. Lint                             │
│    - eslint src                     │
│    - 代码质量检查                    │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│ 4. Package                          │
│    - tsx esbuild.ts --production    │
│    - 打包扩展代码                    │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│ 5. Build VSIX                       │
│    - vsce package                   │
│    - 生成 .vsix 文件                 │
└─────────────────────────────────────┘
```

### 阶段 4: 发布（仅 Release 工作流）

```
┌─────────────────────────────────────┐
│ 1. Create GitHub Release            │
│    - 创建 Release                    │
│    - 生成发布说明                    │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│ 2. Upload VSIX to Release           │
│    - 上传 .vsix 文件                 │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│ 3. Publish to Marketplace           │
│    (仅正式版或手动勾选)               │
│                                     │
│    ├─ VS Code Marketplace           │
│    │  └─ vsce publish               │
│    │                                │
│    └─ Open VSX Registry             │
│       └─ ovsx publish               │
└─────────────────────────────────────┘
```

---

## 🔐 密钥管理

### GitHub Secrets

```
┌─────────────────────────────────────┐
│ GitHub Repository Secrets           │
├─────────────────────────────────────┤
│                                     │
│ GITHUB_TOKEN (自动提供)             │
│ ├─ 用途: 创建 Release               │
│ ├─ 权限: repo                       │
│ └─ 生命周期: 自动管理                │
│                                     │
│ VSCE_TOKEN (手动配置)               │
│ ├─ 用途: 发布到 VS Code Marketplace │
│ ├─ 来源: Azure DevOps PAT           │
│ └─ 权限: Marketplace (Manage)       │
│                                     │
│ OVSX_TOKEN (手动配置)               │
│ ├─ 用途: 发布到 Open VSX Registry   │
│ ├─ 来源: Open VSX Access Token     │
│ └─ 权限: Publish extensions         │
│                                     │
└─────────────────────────────────────┘
```

---

## 📦 产物管理

### 构建产物

```
CI 工作流
    │
    └─→ VSIX 文件
            │
            ├─ 名称: vlinder-vsix-{SHA}
            ├─ 保留: 30 天
            └─ 用途: 临时测试

Release 工作流
    │
    └─→ VSIX 文件
            │
            ├─ 名称: vlinder-vsix-{VERSION}
            ├─ 保留: 90 天
            └─ 用途: 长期存档
```

### 发布产物

```
GitHub Release
    │
    └─→ vlinder-{VERSION}.vsix
            │
            ├─ 永久保存
            └─ 公开下载

VS Code Marketplace
    │
    └─→ Extension Package
            │
            ├─ 永久保存
            └─ 自动分发

Open VSX Registry
    │
    └─→ Extension Package
            │
            ├─ 永久保存
            └─ 自动分发
```

---

## 🔄 缓存策略

### pnpm Store 缓存

```
Cache Key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}

缓存命中
    │
    ├─ pnpm-lock.yaml 未变更
    │       │
    │       └─→ 使用缓存
    │               │
    │               └─→ 快速安装 (~30s)
    │
    └─ pnpm-lock.yaml 已变更
            │
            └─→ 重新安装
                    │
                    └─→ 更新缓存 (~2-3min)
```

---

## 🎯 版本控制策略

### 版本号格式

```
语义化版本: MAJOR.MINOR.PATCH[-PRERELEASE]

示例:
├─ 3.7.22           (正式版)
├─ 3.7.22-beta.1    (Beta 版)
└─ 3.7.22-alpha.1   (Alpha 版)
```

### Git 标签策略

```
标签格式: v{VERSION}

正式版
    │
    └─→ v3.7.22
            │
            ├─ 触发 Release 工作流
            └─→ 自动发布到 Marketplace

测试版
    │
    ├─→ v3.7.22-beta.1
    │       │
    │       ├─ 触发 Release 工作流
    │       └─→ 仅 GitHub Release
    │
    └─→ v3.7.22-alpha.1
            │
            ├─ 触发 Release 工作流
            └─→ 仅 GitHub Release
```

---

## 🚦 发布决策流程

```
推送标签
    │
    ▼
检查标签格式
    │
    ├─ 包含 "beta"?
    │       │
    │       ├─ Yes → 仅 GitHub Release
    │       └─ No → 继续检查
    │
    ├─ 包含 "alpha"?
    │       │
    │       ├─ Yes → 仅 GitHub Release
    │       └─ No → 继续检查
    │
    └─ 正式版本
            │
            └─→ 发布到所有平台
                    │
                    ├─ GitHub Release
                    ├─ VS Code Marketplace
                    └─ Open VSX Registry
```

---

## 📊 监控和日志

### 构建状态监控

```
GitHub Actions Dashboard
    │
    ├─ Workflow Runs
    │   ├─ CI
    │   └─ Release
    │
    ├─ Job Status
    │   ├─ Success ✅
    │   ├─ Failure ❌
    │   └─ In Progress 🔄
    │
    └─ Logs
        ├─ Step Logs
        └─ Error Messages
```

### 发布状态追踪

```
发布完成
    │
    ├─ GitHub Release
    │   └─ https://github.com/QuickerStudio/Vlinder/releases
    │
    ├─ VS Code Marketplace
    │   └─ https://marketplace.visualstudio.com/items?itemName=QuickerStudio.vlinder
    │
    └─ Open VSX Registry
        └─ https://open-vsx.org/extension/QuickerStudio/vlinder
```

---

## 🔧 故障恢复

### 构建失败恢复

```
构建失败
    │
    ├─ 查看日志
    │   └─ 定位错误
    │
    ├─ 修复问题
    │   └─ 提交修复
    │
    └─ 重新触发
        ├─ 推送新提交 → CI 自动运行
        └─ 手动触发 → Actions 页面
```

### 发布失败恢复

```
发布失败
    │
    ├─ Marketplace 发布失败
    │   ├─ 检查 VSCE_TOKEN
    │   ├─ 检查 Publisher ID
    │   └─ 手动发布: vsce publish
    │
    └─ Open VSX 发布失败
        ├─ 检查 OVSX_TOKEN
        ├─ 检查命名空间
        └─ 手动发布: ovsx publish
```

---

## 📈 性能优化

### 构建时间优化

```
优化前: ~5-7 分钟
    │
    ├─ 依赖安装: ~3 分钟
    ├─ 构建: ~2 分钟
    └─ 测试: ~1 分钟

优化后: ~2-3 分钟
    │
    ├─ 依赖安装: ~30 秒 (缓存命中)
    ├─ 构建: ~2 分钟
    └─ 测试: ~1 分钟
```

### 优化策略

1. **pnpm Store 缓存** - 减少依赖安装时间
2. **并行执行** - 多个 job 并行运行
3. **增量构建** - 只构建变更的部分
4. **条件执行** - 跳过不必要的步骤

---

## 🔗 相关文档

- [快速开始指南](QUICKSTART.md)
- [Marketplace 配置](MARKETPLACE_SETUP.md)
- [工作流文档](workflows/README.md)

---

**架构设计原则：**
- ✅ 自动化优先
- ✅ 安全可靠
- ✅ 易于维护
- ✅ 性能优化

