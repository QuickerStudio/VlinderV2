# Prompt Editor 提示词编辑器

智能提示词编辑器组件，提供增强的文本编辑体验，支持语法高亮、智能提示、工具集成等功能。

## 📋 组件清单

| 组件 | 文件 | 描述 |
|------|------|------|
| PromptEditor | `index.tsx` | 提示词编辑器主组件 |
| PromptActions | `prompt-actions.ts` | 提示词相关的操作和逻辑 |
| Tools | `tools.tsx` | 编辑器工具栏组件 |
| Utils | `utils.ts` | 编辑器工具函数 |

## 🎯 主要功能

### ✏️ 编辑功能
- **语法高亮** - 支持Markdown和代码语法高亮
- **智能缩进** - 自动缩进和格式化
- **括号匹配** - 自动匹配括号和引号
- **多光标编辑** - 支持多光标同时编辑

### 🔍 智能提示
- **自动补全** - 基于上下文的智能补全
- **模板插入** - 预定义的提示词模板
- **变量提示** - 可用变量的智能提示
- **函数签名** - 工具函数的参数提示

### 🛠️ 工具集成
- **格式化工具** - 一键格式化提示词
- **模板管理** - 保存和管理常用模板
- **历史记录** - 编辑历史的撤销/重做
- **导入导出** - 提示词的导入和导出

### 🎨 用户体验
- **实时预览** - 实时预览渲染效果
- **快捷键支持** - 丰富的键盘快捷键
- **主题切换** - 支持多种编辑器主题
- **字体设置** - 可调节字体大小和类型

## 🏗️ 架构设计

### 组件结构
```
PromptEditor
├── EditorToolbar (工具栏)
│   ├── FormatButton (格式化)
│   ├── TemplateButton (模板)
│   └── SettingsButton (设置)
├── EditorArea (编辑区域)
│   ├── SyntaxHighlighter (语法高亮)
│   ├── AutoComplete (自动补全)
│   └── LineNumbers (行号)
└── StatusBar (状态栏)
    ├── CursorPosition (光标位置)
    ├── WordCount (字数统计)
    └── EncodingInfo (编码信息)
```

### 数据流
```
用户输入 → 语法分析 → 智能提示 → 内容更新 → 实时预览
```

## 🔧 开发指南

### 添加新的语法高亮
```tsx
// 在语法高亮配置中添加新语言
const syntaxConfig = {
  // 现有配置...
  newLanguage: {
    keywords: ['keyword1', 'keyword2'],
    operators: ['+', '-', '*', '/'],
    strings: ['"', "'"],
    comments: ['//', '/*', '*/']
  }
}
```

### 扩展自动补全
```tsx
// 添加新的补全提供者
const completionProvider = {
  provideCompletionItems: (model, position) => {
    return [
      {
        label: 'newTemplate',
        kind: CompletionItemKind.Snippet,
        insertText: 'Template content...',
        documentation: '新模板的描述'
      }
    ]
  }
}
```

### 自定义工具
```tsx
// 创建新的编辑器工具
function CustomTool() {
  const handleAction = () => {
    // 工具逻辑
  }
  
  return (
    <Button onClick={handleAction}>
      自定义工具
    </Button>
  )
}
```

## 🎨 主题和样式

### 支持的主题
- **VS Code Dark** - 深色主题
- **VS Code Light** - 浅色主题
- **Monokai** - 经典深色主题
- **GitHub** - GitHub风格主题

### 自定义样式
```css
.prompt-editor {
  --editor-bg: #1e1e1e;
  --editor-fg: #d4d4d4;
  --editor-selection: #264f78;
  --editor-line-number: #858585;
}
```

## 🔗 相关组件
- [ui/](../ui/) - 基础UI组件
- [chat-view/](../chat-view/) - 聊天界面集成
- [code-block/](../code-block/) - 代码块显示
