# Thumbnails 缩略图组件

文件和内容的缩略图显示组件，提供快速预览功能。

## 📋 组件清单

| 组件 | 文件 | 描述 |
|------|------|------|
| Thumbnails | `thumbnails.tsx` | 缩略图容器组件 |
| ThumbnailItem | `thumbnail-item.tsx` | 单个缩略图项组件 |

## 🎯 主要功能

### 🖼️ 缩略图显示
- **图片预览** - 图片文件的缩略图
- **文档预览** - 文档文件的预览图
- **代码预览** - 代码文件的语法高亮预览
- **视频预览** - 视频文件的封面图

### 🔧 交互功能
- **点击预览** - 点击查看完整内容
- **拖拽操作** - 支持拖拽排序
- **批量选择** - 多选操作支持
- **右键菜单** - 丰富的操作菜单

### 📱 响应式设计
- **网格布局** - 自适应网格布局
- **尺寸调整** - 可调节缩略图尺寸
- **移动适配** - 移动端友好的交互

## 🔧 开发指南

### 基础使用
```tsx
import { Thumbnails, ThumbnailItem } from './thumbnails'

function Gallery() {
  const items = [
    { id: '1', type: 'image', src: '/image1.jpg', title: '图片1' },
    { id: '2', type: 'document', src: '/doc1.pdf', title: '文档1' },
    { id: '3', type: 'code', src: '/code1.js', title: '代码1' }
  ]
  
  return (
    <Thumbnails>
      {items.map(item => (
        <ThumbnailItem
          key={item.id}
          item={item}
          onClick={() => openPreview(item)}
        />
      ))}
    </Thumbnails>
  )
}
```

## 🔗 相关组件
- [chat-view/](../chat-view/) - 聊天中的文件预览
- [ui/](../ui/) - 基础UI组件
