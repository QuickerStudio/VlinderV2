# 连接线类型测试

## 测试用例
```mermaid
graph TD
    A --> B
    B -.- C
    C -..-> D
    D |是|--> E
    E |否|--> F
    F |失败|--> G
    G ==> H
    H -.-> I
    I --- J
    J === K
```

## 预期结果
- 所有连接线类型都应该被正确识别
- 右键菜单应该能显示所有连接线
- 连接线样式更改应该正常工作
- 带标签的连接线格式应该正确

## 问题修复
1. ✅ 修复正则表达式识别 `-.-` 和 `-..->` 类型
2. ✅ 修复带标签连接线格式 `-->|label|` 
3. ✅ 更新 EDGE_TYPES 配置
4. ✅ 完善解析和生成逻辑
